#!/bin/bash -ex

sleep $[RANDOM%300]

IS_MASTER=`mongo --quiet --eval "d=db.isMaster(); print( d['ismaster'] );"`

if [ "$IS_MASTER" != 'true' ]; then

	rs_run_recipe --name 'rsc_mongodb::mongodb_backup'

fi
